#!/bin/bash
# Script: mode
# Description: Script for handling mode changes

case $mode in
	'+o') mode="@";;
	'+v') mode="+";;
	*) mode=" ";;
esac

sed -i "s/^[@|+| ]$mode_nick$/$mode$mode_nick/" var/run/$server/$chan
